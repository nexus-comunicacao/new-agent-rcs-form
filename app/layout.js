export const metadata = {
  title: 'Abertura de Agente RCS — NEXUS Comunicação',
  description: 'Formulário de abertura de novo agente RCS',
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
